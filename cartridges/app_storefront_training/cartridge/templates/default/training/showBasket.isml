
<h1>Basket items are :</h1>
<br/>
<isloop items="${pdict.currentBasket.getProductLineItems}" var="productLineItem" status="status">
    <h2 >${productLineItem.product.name} </h2>
    <h3> indexStatus:${status.index}</h3>
    <h3> countStatus:${status.count}</h3>
</isloop>